/*
 * data.h — generated by scripts/extract_kb_problem.py.
 *
 * Input 0 shape: (1, 8, 16, 16)
 * Output shape: (1, 16, 14, 14)
 */

#pragma once

#include <stddef.h>

#define NUM_SETS                    5
#define NUM_INPUTS                  1
#define IN0_NUMEL                   2048
#define IN_NUMEL                    2048
#define OUT_NUMEL                   3136

/* Precomputed ExecuTorch+XNNPACK baseline (total cycles across NUM_SETS
 * invocations on this hardware config). The harness reports this as
 * iter-0's `ID 0 latency:` so beam-search keep_factor uses ExecuTorch's
 * actual cost as the reference. Override at extraction time via
 * `--baseline-cycles N`. */
#define BASELINE_EXECUTORCH_CYCLES  50000000

/* Per-set inputs (5 seeded random tensors per forward() arg). Each entry
 * is a pointer to a 16-byte-aligned byte array reinterpreted as float*. */
extern const float * const inputs0[NUM_SETS];

/* PyTorch-computed expected outputs (one per input set, baked at extraction
 * time via Model.forward()). The bit-exact correctness oracle in the harness. */
extern const float * const expected_outputs[NUM_SETS];

/* Model parameters (extracted from named_parameters(), in declaration order). */
extern const float * const conv_weight;  /* shape (16, 8, 3, 3), 4608 bytes */
extern const float * const conv_bias;  /* shape (16,), 64 bytes */
