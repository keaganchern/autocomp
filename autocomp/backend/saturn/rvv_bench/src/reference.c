// Auto-generated per-build by saturn_eval._build_and_run_spike.
// At source-tree state this is an empty stub so the CMakeLists compiles
// against an unprepared slot; the real contents (sol renamed to
// `reference_kernel`) are written into app/src/reference.c at evaluation time.
